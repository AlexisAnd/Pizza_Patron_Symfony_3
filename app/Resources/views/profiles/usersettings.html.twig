{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <h2 class="mt-4 mb-5 text-center">Votre Compte</h2>
        <div class="row">
            <div class="col"></div>
            <div class="col-8">
                <div class="border border-secondary border-right-0 border-left-0 pt-4 pb-4">
                <h4>Vos coordonnées</h4>
        <ul class="list-unstyled">
            <li>{{ app.user.firstName }} {{ app.user.lastName }}</li>
            <li>{{ app.user.address }}</li>
            <li>{{ app.user.city }}, {{ app.user.postalCode }}</li>
            <li>{{ app.user.phone }}</li>
        </ul>
        <a class="btn btn-info mb-4" role="button" href="{{ url('app.profiles.update') }}">Modifier l'adresse de livraison</a>
            </div>

            <div class="border border-secondary border-top-0 border-right-0 border-left-0">
         <h4 class="mt-4">Mot de passe</h4>
            <p>Vous devez d'abord rentrer votre mot de passe actuel pour pouvoir le modifier</p>
            <form action="{{ path('app.profiles.usersettings') }}" method="post">
                <label for="password">Mot de passe:  </label>
                <input class="mb-5" type="password" id="password" name="_password" />

                <button type="submit" class="btn btn-info">Editer</button>
            </form>
            </div>
            </div>
            <div class="col"></div>
        </div>
    </div>
    {% include "inc/footer.html.twig" %}

{% endblock %}